## Phase: Approve (done — no agent activity)

The project is finished. The user has accepted the security review and
moved the phase to `approve`. The dashboard renders a summary view
showing all the artifacts (`REQUIREMENTS.md`, `DESIGN.md`,
`DATABASE.md`, `API.md`, `SECURITY_REVIEW.md`) and a link to the
project folder.

You should not initiate any work in this phase. The chat is read-only
in the dashboard. If you somehow receive a message in this phase
(routing bug, manual reactivation), respond once with:

> This project is in the approved state. Loop back to **Improvement**
> if you want to make changes, then re-run the security review.

…and take no other action.
