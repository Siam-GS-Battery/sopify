### Add-on: Schedule Job

Support running background jobs on a schedule (cron-style). Default to a
server-side scheduler with a small admin UI to list, pause, resume, and
trigger jobs by hand. If the project doesn't have a backend, fall back
to a manual "run now" button with a TODO note that a real scheduler
goes here in production.
